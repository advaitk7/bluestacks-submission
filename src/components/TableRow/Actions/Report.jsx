import ActionButton from "./ActionButton";

export default function ReportAction({ imagePath }) {
  return (
    <ActionButton image={imagePath + "/Group 2/statistics-report.png"} text={"Report"} />
  )
}